#ifndef TICK_TEST_SPIFFSINIFILE_H
#define TICK_TEST_SPIFFSINIFILE_H
// The protocol driver interface mentions this type in a function pointer
// signature; the capture tests never call through it.
class SPIFFSIniFile {};
#endif
